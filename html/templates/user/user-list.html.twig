{% extends 'base.html.twig' %}
{% block body %}
    <h1>Users ( {{ users|length }} )</h1>
    <hr>
    <div class="list">
        {% for user in users %}
            {% include 'user/user-miniprofile.html.twig' %}
        {% endfor %}
    </div>
{% endblock %}