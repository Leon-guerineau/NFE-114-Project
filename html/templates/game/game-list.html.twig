{% extends 'base.html.twig' %}
{% block body %}
    <h1>games ( {{ games|length }} )</h1>
    <hr>
    <div class="list">
        {% for game in games %}
            {% include 'game/game-miniprofile.html.twig' %}
        {% endfor %}
    </div>
{% endblock %}